import type { TFunction } from 'i18next';
import type { User } from '../../../../../auth';
import type { SanitizedConfig } from '../../../../../config/types';
import type { Field as FieldSchema } from '../../../../../fields/config/types';
import type { Data, Fields } from '../types';
type Args = {
    config: SanitizedConfig;
    data: Data;
    fields: FieldSchema[];
    fullData: Data;
    id: number | string;
    locale: string;
    operation: 'create' | 'update';
    parentPassesCondition: boolean;
    path: string;
    preferences: {
        [key: string]: unknown;
    };
    state: Fields;
    t: TFunction;
    user: User;
};
export declare const iterateFields: ({ id, config, data, fields, fullData, locale, operation, parentPassesCondition, path, preferences, state, t, user, }: Args) => Promise<void>;
export {};
//# sourceMappingURL=iterateFields.d.ts.map